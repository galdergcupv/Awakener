<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- App name text -->
    <TextView
        android:id="@+id/app_name_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="24sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"/>

    <!-- "No alarms programmed" text -->
    <TextView
        android:id="@+id/empty_list_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/empty_list_message"
        android:textSize="16sp"
        android:textStyle="bold"
        android:background="@drawable/border_empty_list"
        android:padding="32dp"
        app:layout_constraintTop_toBottomOf="@id/app_name_textview"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:visibility="gone"/>

    <!-- Fragment Container -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/empty_list_textview"
        app:layout_constraintBottom_toTopOf="@id/bottom_task_bar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Bottom task bar -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/bottom_task_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        android:elevation="8dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Home button -->
        <ImageButton
            android:id="@+id/home_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:src="@drawable/baseline_access_alarms_24"
            android:background="?android:selectableItemBackground"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/add_alarm_button"
            app:layout_constraintTop_toTopOf="parent"
            android:padding="16dp"/>

        <!-- Add alarm button -->
        <ImageButton
            android:id="@+id/add_alarm_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:src="@drawable/baseline_add_alarm_24"
            android:background="?android:selectableItemBackground"
            app:layout_constraintStart_toEndOf="@id/home_button"
            app:layout_constraintEnd_toStartOf="@id/activities_button"
            app:layout_constraintTop_toTopOf="parent"
            android:padding="16dp"/>

        <!-- Activities button-->
        <ImageButton
            android:id="@+id/activities_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:src="@drawable/baseline_games_24"
            android:background="?android:selectableItemBackground"
            app:layout_constraintStart_toEndOf="@id/add_alarm_button"
            app:layout_constraintEnd_toStartOf="@id/settings_button"
            app:layout_constraintTop_toTopOf="parent"
            android:padding="16dp"/>

        <!-- Settings button-->
        <ImageButton
            android:id="@+id/settings_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:src="@drawable/baseline_build_24"
            android:background="?android:selectableItemBackground"
            app:layout_constraintStart_toEndOf="@id/activities_button"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:padding="16dp"/>

    </androidx.constraintlayout.widget.ConstraintLayout>




</androidx.constraintlayout.widget.ConstraintLayout>
